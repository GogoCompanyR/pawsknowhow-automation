# Content generation
anthropic>=0.92.0

# Source fetching (AKC scrape)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Blogger publishing (Google APIs + OAuth)
google-api-python-client>=2.120.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
# Pinned: this machine's Python 3.9 links OpenSSL 1.1.1g; cryptography>=42 needs
# OpenSSL 3.x (symbol _ERR_get_error_all) and fails to import. 38.0.4 is the last
# line compatible with 1.1.1 and satisfies google-auth's >=38.0.3 floor.
cryptography==38.0.4

# Hero image embedding (downscale breed PNG -> inline base64 JPEG)
Pillow>=10.0.0

# Config + frontmatter
python-dotenv>=1.0.0
PyYAML>=6.0.1
